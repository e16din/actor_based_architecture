<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="120dp"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:background="@drawable/yellow_gray_rounded_shape"
    android:clickable="true"
    android:focusable="true"
    android:alpha="1"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/carTypeImage"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center_horizontal"
        app:srcCompat="@drawable/ic_car_business" />

    <TextView
        android:id="@+id/carTypeLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:textColor="@color/inactive_order_type_name_color"
        android:textSize="12sp"
        tools:text="@string/car_type_light" />

    <TextView
        android:id="@+id/priceLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="8dp"
        android:textColor="@color/black"
        android:textSize="16sp"
        tools:text="245 ₽" />

</LinearLayout>

        